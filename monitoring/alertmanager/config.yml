route:
  receiver: 'slack'
  group_by: ['alertname', 'service']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'slack'
    webhook_configs:
      - url: 'https://example.invalid/webhook'
        send_resolved: true
